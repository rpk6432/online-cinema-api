services:
  app:
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
    volumes:
      - ./src:/app/src

  celery-worker:
    volumes:
      - ./src:/app/src

  celery-beat:
    volumes:
      - ./src:/app/src
